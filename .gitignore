node_modules/

# rust build artifacts
target/

# secrets — never commit
.env
.env.*

# No config or runtime state lives in this repo — the deployment follows the
# XDG base dirs (see docs/layout.md). ROSTER_ROOT gives a self-contained root.
