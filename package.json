{
  "name": "roster",
  "version": "0.0.1",
  "private": true,
  "description": "pi (the engine) is the only Node dependency — mounted read-only into the box. All host-side code is Rust (the `roster` binary). Run `npm install` once to provide pi to the box.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "cargo test --manifest-path gateway/Cargo.toml"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.80.3"
  }
}
